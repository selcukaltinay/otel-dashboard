global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    cluster: 'production'
    monitor: 'otel-monitoring'

# Alertmanager configuration (opsiyonel)
# alerting:
#   alertmanagers:
#     - static_configs:
#         - targets:
#           - alertmanager:9093

# Rule dosyaları (opsiyonel)
# rule_files:
#   - "alerts/*.yml"

scrape_configs:
  # OpenTelemetry Collector Gateway'in kendi metrikleri
  - job_name: 'otel-collector-gateway-internal'
    static_configs:
      - targets: ['localhost:8890']
        labels:
          service: 'otel-collector-gateway'
          type: 'internal-metrics'

  # OpenTelemetry Collector tarafından export edilen uygulama metrikleri
  - job_name: 'otel-application-metrics'
    static_configs:
      - targets: ['localhost:8889']
        labels:
          collector_type: 'gateway'

  # Prometheus kendi metrikleri
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']
        labels:
          service: 'prometheus'

  # Grafana metrikleri (eğer expose ediliyorsa)
  - job_name: 'grafana'
    static_configs:
      - targets: ['localhost:3000']
        labels:
          service: 'grafana'

  # Zipkin metrikleri
  - job_name: 'zipkin'
    static_configs:
      - targets: ['localhost:9411']
        labels:
          service: 'zipkin'

  # Node exporter varsa (sistem metrikleri için)
  # - job_name: 'node-exporter'
  #   static_configs:
  #     - targets: ['node-exporter:9100']
