# OpenTelemetry Collector - Gateway Mode Configuration
# Bu collector merkezi monitoring node'da çalışır ve tüm verileri toplar

receivers:
  # App node'lardan gelen veriler
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
      http:
        endpoint: 0.0.0.0:4318

processors:
  batch:
    timeout: 10s
    send_batch_size: 2048
    send_batch_max_size: 4096

  memory_limiter:
    check_interval: 1s
    limit_mib: 1024
    spike_limit_mib: 256

  # Resource processor - attribute ekleme
  resource:
    attributes:
      - key: environment
        action: upsert
        value: production

exporters:
  # Prometheus exporter - metrikler için
  prometheus:
    endpoint: "0.0.0.0:8889"
    namespace: otel
    send_timestamps: true
    metric_expiration: 5m
    resource_to_telemetry_conversion:
      enabled: true

  # Zipkin exporter - trace'ler için
  zipkin:
    endpoint: "http://zipkin:9411/api/v2/spans"
    format: proto

  # Debug exporter (debug/monitoring için)
  debug:
    verbosity: detailed
    sampling_initial: 5
    sampling_thereafter: 100

  # Prometheus Remote Write (opsiyonel - uzun süreli depolama için)
  # prometheusremotewrite:
  #   endpoint: "http://prometheus:9090/api/v1/write"
  #   tls:
  #     insecure: true

extensions:
  health_check:
    endpoint: 0.0.0.0:13133
    path: "/health"
    check_collector_pipeline:
      enabled: true
      interval: 5m
      exporter_failure_threshold: 5

  pprof:
    endpoint: 0.0.0.0:1777

  zpages:
    endpoint: 0.0.0.0:55679

service:
  extensions: [health_check, pprof, zpages]

  pipelines:
    # Trace pipeline
    traces:
      receivers: [otlp]
      processors: [memory_limiter, batch, resource]
      exporters: [zipkin, debug]

    # Metrics pipeline
    metrics:
      receivers: [otlp]
      processors: [memory_limiter, batch, resource]
      exporters: [prometheus, debug]

    # Logs pipeline
    logs:
      receivers: [otlp]
      processors: [memory_limiter, batch, resource]
      exporters: [debug]
